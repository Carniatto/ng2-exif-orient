import { ExifDirective } from './exif.directive';

describe('ExifDirective', () => {
  it('should create an instance', () => {
    const directive = new ExifDirective();
    expect(directive).toBeTruthy();
  });
});
